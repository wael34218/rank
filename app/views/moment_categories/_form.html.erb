<%= form_for(@moment_category) do |f| %>
  <% if @moment_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moment_category.errors.count, "error") %> prohibited this moment_category from being saved:</h2>

      <ul>
      <% @moment_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :photo%><br>
    <%= f.file_field :photo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
