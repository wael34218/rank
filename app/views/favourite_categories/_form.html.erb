<%= form_for(@favourite_category) do |f| %>
  <% if @favourite_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favourite_category.errors.count, "error") %> prohibited this favourite_category from being saved:</h2>

      <ul>
      <% @favourite_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :photo%><br>
    <%= f.file_field :photo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
